# Handoff 2026-08-29 — Kollektivtraum (Tool 2) + Anzeigeregler (Tool 1)

Nachfolger von `docs/HANDOFF-2026-08-26.md`. Dessen Punkte A–G sind
**vollständig abgearbeitet** — Stand, Entscheidungen und was offen bleibt
stehen hier.

**Repo:** `~/projekte/kollektivgedaechtnis`, Branch `master`, alles committet
und gepusht. **Testsuite:** 738 passed, 3 deselected (voller Lauf ~20 min).
**Festival:** NEW bauhaus, 2.–3. September 2026, Weimarhalle.

---

## 1. Was heute entschieden und gebaut wurde

### Stufe 1 (Satz) — `kg2/condense.py`

| Was | Entscheidung |
|---|---|
| Satzform | **Ein Hauptsatz, max. 16 Wörter, kein Komma.** Gemessen: 36 Wörter = 4 Zeilen = ~11 s Lesezeit; ≤16 Wörter = 1–2 Zeilen. |
| Stufigkeit | **Einstufig.** Der angezeigte Satz ist derselbe, den das Bild bekommt. |
| Leitfrage | **Entfällt im Prompt.** Ersetzt durch eine neutrale Verdichtungsanweisung. Die Config-Option steuert ab jetzt **nur noch die Überschrift auf dem Schirm**. |
| Widerspruchs-Klausel | **Entfernt**, ersetzt durch eine Belegbarkeits-Klausel: alles muss sich auf die Begriffe stützen. |
| Sprache | **Zweisprachig** — deutscher Satz für die Wand, englische Übersetzung für den Bildprompt. |
| Analysewerte | **`mood` und `tension`, je 1–5**, im selben LLM-Aufruf erzeugt. Hart geklemmt nach dem Aufruf. |

### Material — `kg2/weighting.py`

- **Gleitende Begriffsauswahl:** alle geteilten Begriffe (≥2 Nennungen) immer,
  aufgefüllt mit höchstens `SINGLE_MENTION_BUDGET` (20) Einmal-Nennungen — die
  **jüngsten** zuerst. Das Budget schrumpft linear auf 0, während die geteilten
  von 0 auf `SHARED_TERMS_SATURATION` (25) wachsen. Kein Umschaltpunkt, kein
  Zähler, kein Zustand in der DB.
- **Kopfzeile entfernt** („Der Graph umfasst N Menschen…") — brachte nichts und
  verleitete das Modell, die Zahl in den Satz zu schreiben.
- **Zitate entfernt.** Sie machten bei 60 Personen 76 % des Materialblocks aus.
  Begründung: Auf der Wand sind nur die Begriffe sichtbar; drei Viertel des
  Prompts dürfen nicht aus etwas im Raum Unsichtbarem bestehen.
- **Aktualitätsblock „Zuletzt gesagt"** (`RECENT_TERMS` = 5): die fünf jüngsten
  Begriffe, aus geteilten UND einmaligen. Zweite Achse neben der Häufigkeit.

### Bildprompt — `kg2/imagegen.py`

Fünf Bausteine, **komplett englisch**, zusammenhängende Prosa (Google-Doku:
„A simple list of keywords won't cut it"):

```
<englischer Satz>      Motiv, variabel
<Stimmung>             aus mood, 5 feste Formulierungen, NUR Licht und Farbe
<Spannung>             aus tension, 5 feste Formulierungen, nur Kohärenzgrad
<Register>             fix für den ganzen Tag
<Format>               fix
```

Im Register neu und fix: neutrale Kamera-/Objektivangabe (Augenhöhe,
Normalbrennweite), ausformulierte Schriftfreiheit, Einzelbild-Hinweis.
**Durchgehend positiv formuliert** — alle 25 mood/tension-Kombinationen sind
verneinungsfrei (vorher acht Verneinungen im Prompt).

### Tool 1

- **Genau ein Zitat pro Person**, hart gekappt nach dem LLM-Aufruf.
- **Zwei Merge-Bugs** behoben (verworfene Ein-Member-Gruppe, toter Alias).
- **JPEG-Bug** behoben, inkl. Überschreibschutz.
- **Anzeigeregler `min_mentions` → `max_terms`** (Standard 32), gleiche
  Auswahlregel wie Tool 2, plus Mindeststandzeit von 3 Aktualisierungen.

---

## 2. Belegte Messungen (nicht wiederholen, es sei denn etwas ändert sich)

**Stimmungsskala.** Vier gebaute Extreme, je dreimal: `mood` trifft 5/3/3/1
sauber und reproduzierbar.

**Spannungsskala.** Der erste Test war **ungültig** (der „zerstrittene" Fall war
in Wahrheit einhellig negativ). Der Nachtest entkoppelt beide Achsen im
2×2-Raster:

| | einig | zerstritten |
|---|---|---|
| positiv | tension 1,1,1 | **tension 5,5,5** |
| negativ | tension 2,2,2 | tension 4,2,4 |

Widerspruchs-Achse **+2,67**, Stimmungs-Achse **−0,33**. Der Effekt ist
achtmal so groß und gegenläufig — `tension` misst Widerspruch, nicht Stimmung.

**Bildkanal wirkt.** Fünf Testbilder (`sim/probes/moodgrid.py`,
`out/moodgrid/`): Farbtemperatur läuft monoton von −21 (mood=1) bis +61
(mood=5) Rot-minus-Blau.

**Wand-Lesbarkeit.** Bei `min_mentions=1` und 60 Personen: 163 Begriffe, 56
überlappende Label-Paare, 51 Labels auf Portraits. Mit Obergrenze: **null
Kollisionen** über den ganzen Bereich 20–60, Schrift sinkt monoton von 17,2 auf
7,5 px.

**Reihenfolge-Effekt.** Nicht über dem Rauschen nachweisbar (n=6). **Shuffle
wurde bewusst nicht gebaut** — er hätte die Reproduzierbarkeit gekostet ohne
belegten Nutzen.

**N/X (20/25).** Ab 30 Personen wirkungslos, darunter kein lesbarer
Qualitätsunterschied. Werte **gesetzt statt kalibriert**, mit der Messung als
Beleg, dass das zulässig ist.

---

## 3. Zwei eigene Fehler, dokumentiert damit sie nicht wiederkehren

**Kaputte Messmethode.** Die erste Lesbarkeitsmessung öffnete die
Fixture-Datenbank in place statt über eine Scratch-Kopie und wartete eine feste
Zeit statt auf `layoutPending === false`. Ergebnis: eine eingefrorene
Kamera-Ansicht aus dem vorherigen Lauf wurde mitgemessen, woraus die absurde
Behauptung „weniger Begriffe ergeben kleinere Schrift" entstand. Die Spec
behält den widerlegten Befund **sichtbar als widerlegt**, statt ihn zu löschen.
Die Sonde ist repariert.

**Umgedrehte Semantik in Namen.** Beim Reglerumbau hieß eine Testsequenz
weiter `dial-2-to-1`. Unter `min_mentions` bedeutete das *mehr* Begriffe, unter
`max_terms` *weniger* — die Bedeutung drehte sich um, der Name wanderte mit.
Fiel nur auf, weil ein Test rot wurde. Namen jetzt nach echten Begriffszahlen.

---

## 4. Was offen ist

> **Ziel der nächsten Sitzung (Birk, 2026-08-29): so schnell wie möglich ein
> kompletter prototypischer Probedurchlauf.** Die Punkte unten sind danach
> sortiert, was dafür wirklich im Weg steht.

### ✅ Erledigt am 2026-08-29 (nachmittags) — der Probedurchlauf ist entblockt

1. **End-to-End-Test für Tool 2 gebaut und real gefahren.**
   `tests/e2e/test_interview_to_screen_b.py`. Die Kette **Interview → Traum →
   Bild → Screen B ist am 2026-08-29 zum ersten Mal als Ganzes gelaufen**,
   gegen echte Cloud (ein Anthropic-Aufruf, ein OpenRouter-Bild), 32 s,
   PNG 1376×768. Nichts zwischen den Werkzeugen ist gestubbt: Tool 1 läuft
   als echter uvicorn auf einem echten Port, Tool 2 pollt `graph.json` über
   echtes HTTP. Neun Schritte, darunter die drei, die vorher niemand
   nachgewiesen hatte: dass `save_image` das Format aus den Bytes ableitet,
   dass ein Neustart dasselbe Material **nicht** noch einmal träumt, und dass
   ein Interview innerhalb des Mindestabstands **verzögert statt verworfen**
   wird.

   ```bash
   set -a; . ~/.hermes/.env; set +a
   export ANTHROPIC_BASE_URL=http://127.0.0.1:28764 ANTHROPIC_API_KEY=proxy
   uv run pytest -m e2e tests/e2e/test_interview_to_screen_b.py -v -s
   ```

   Kostet ≈ 0,14 USD pro Lauf (ein Bild). Ohne beide Schlüssel überspringt er
   sich, statt grün zu werden — ein Lauf, der die Cloud stumm gestubbt hätte,
   wäre wertlos.

   Erster Satz aus der Kette: *„Der leere Dorfkern trägt seine graue Energie
   als Recyclingbeton weiter und verteilt Materialpässe unter
   Photovoltaikdächern"* (mood 4, tension 2). Bild in
   `out/e2e-probedurchlauf/d1.png`.

2. **Bildvertrag: war schon verifiziert — der Handoff war die Leiche.**
   `docs/dream-image-contract.md` trägt seit dem 2026-08-26 „VERIFIZIERT"
   (drei echte Aufrufe, Commit `85cce5b`), inklusive der drei gefundenen
   Abweichungen. Offen war nur noch der veraltete Kommentar „NOCH NICHT
   VERIFIZIERT" in `config2.example.toml` — der ist jetzt korrigiert. Der
   E2E-Lauf oben hat den Vertrag zusätzlich ein viertes Mal bestätigt.

3. **Gemeinsamer Start dokumentiert und mit einem Startskript belegt.**
   Neu: `scripts/start-dream.sh` (das Gegenstück zu `scripts/start.sh`) und
   der Runbook-Abschnitt „Beide Werkzeuge zusammen starten" in
   `docs/operations.md`. Der Ablauf ist **bewusst keine Abhängigkeit**: Das
   Skript prüft `tool1_url` aus `config2.toml`, sagt in beiden Richtungen
   klar, was es findet — und startet Screen B trotzdem, wenn Tool 1 fehlt.
   Beide Zweige sind gegen einen laufenden bzw. abwesenden Tool-1-Server
   real geprüft. `tests/test_dream_runbook.py` bewacht das jetzt mit sechs
   zusätzlichen Tests (Skripte existieren und sind ausführbar, beide
   Schlüssel werden vorab geprüft, kein `exit` im Nicht-erreichbar-Zweig).

**Beide Tools auf einer Kiste ist für den Probedurchlauf zulässig** — Ports
(8800/8810) und Datenverzeichnisse (`data/` vs. `dream-data/`) kollidieren
nicht. Die Trennung auf zwei Maschinen ist eine Ausfall-Entscheidung, keine
technische Notwendigkeit.

### 🔴 Blockiert den Probedurchlauf

*Nichts mehr.* Die drei Punkte oben sind abgearbeitet; was noch offen ist,
sind Entscheidungen am Material und Dinge, die den Raum brauchen.

**Was für einen echten Probedurchlauf mit 10–15 Interviews jetzt noch fehlt,
ist kein Code, sondern das Material:** Interviews müssen tatsächlich
eingesprochen (oder über `sim/` eingespielt) werden. Der E2E-Test beweist die
Kette an einem seed-Graphen; er ersetzt nicht den Durchlauf mit echten
Menschen davor.

### Zurückgestellt (unverändert offen)

**Grobrechnung für den Probedurchlauf:** Bei `min_interval_s = 240` und 60
Interviews über 8 h entstehen ~60 Träume/Tag → **~8,34 USD Bildkosten pro
Ausstellungstag**, ~16,70 USD für beide Tage. Ein Probedurchlauf mit 10–15
Interviews kostet entsprechend 1,40–2,10 USD.

### Vor der Ausstellung zwingend

4. **Reglerstufen vor Ort festlegen.** Die Messung deckt 20–60 ab; welche
   Stufe am echten Beamer richtig ist, entscheidet der Raum. Spec §4.
5. **Kamera, Zoom und Tempo vor Ort** (Entscheidung D4, unverändert offen).

### Inhaltlich offen — Birks Entscheidung

6. **Bildsprache insgesamt** (Handoff-Punkt F). Bewusst zurückgestellt, bis
   Bilder aus dem neuen Aufbau vorliegen — die gibt es jetzt (`out/moodgrid/`).
   Die Frage: Wie verhält sich eine freundlichere Bildsprache zum kritischen
   Anspruch von Spec §1?
7. **Leitfrage** (Punkt D): Kandidat B steht in der Config. Das Programm der
   NEW bauhaus 2026 wurde durchgesehen — 15 von ~45 Punkten fragen nach
   Macht/Verantwortung/Konflikt, keine einzige Jahreszahl als Zielhorizont.
   Die Frage steuert jetzt nur noch die Überschrift, ist also eine reine
   Textentscheidung.
8. **Zitate endgültig bestätigen.** Default ist raus; die Gegenüberstellung
   liegt in `out/calibrate-quotes.txt`. Die Sätze **mit** Zitaten haben etwas
   mehr Bewegung — der Unterschied ist kleiner als befürchtet.
9. **40-Bilder-Serie** (~5,55 USD) — braucht Birks OK.
10. **Vergleichsbilder „Weg 2"**: Stimmungstexte vom Modell frei formulieren
    lassen statt fest hinterlegt. Birk wollte das später einmal sehen.

### Technisch offen

11. **Abgebrochene Sätze.** Ein Satz brach mitten im Wort ab. 71 reale
    API-Aufrufe ohne Reproduktion — Ursache wahrscheinlich auf Modell-/Proxy-
    Ebene, **nicht belegt**. Erkennung ist gebaut (Steuerzeichen im Text →
    Traum `failed`, vorheriges Bild bleibt), die Ursache bleibt offen.
12. **Fall D der tension-Skala** streute einmal (2 statt 4). Drei von vier
    Fällen sind exakt reproduzierbar.

---

## 5. Wo was liegt

| Was | Wo |
|---|---|
| Runbook, kalibrierte Werte, offene Entscheidungen | `docs/operations.md` |
| Spec Kollektivtraum | `docs/superpowers/specs/2026-08-25-kollektivtraum-brainstorm.md` |
| Spec Anzeigeregler | `docs/superpowers/specs/2026-08-29-wand-anzeigeregler-begriffsobergrenze.md` |
| Kalibrierläufe | `out/calibrate-{terms,mood,tension,quotes,recency}.txt` |
| Testbilder | `out/moodgrid/`, Kontaktkarte `UEBERSICHT-moodgrid.png` |
| Messsonden (ad hoc, nicht testabgedeckt) | `sim/probes/` — `wall_legibility.py`, `moodgrid.py` |
| Kalibrierwerkzeug (gepflegt) | `sim/dream_calibrate.py` |
| End-to-End-Tests | `tests/e2e/`, opt-in mit `pytest -m e2e tests/e2e` — zwei für Tool 1 (Telegram-Foto, Sprache→Wand), einer für die ganze Kette bis Screen B (`test_interview_to_screen_b.py`, kostet ≈ 0,14 USD) |
| Startskripte | `scripts/start.sh` (Tool 1), `scripts/start-dream.sh` (Tool 2); gemeinsamer Ablauf in `docs/operations.md` |
| Bauprotokoll Tool 2 (erledigt, teils überholt) | `docs/superpowers/plans/2026-08-25-kollektivtraum-tool2.md` |

**Kalibrierläufe brauchen:**
```bash
export ANTHROPIC_BASE_URL=http://127.0.0.1:28764
export ANTHROPIC_API_KEY=proxy
```
Nur vor dem konkreten Befehl setzen, nicht in die eigene Shell exportieren.

---

## 6. Zwei Arbeitshinweise für die nächste Sitzung

**Die Testsuite selbst fahren, nicht delegieren.** Claude Codes Bash-Werkzeug
kappt bei ~10 Minuten und schiebt längere Läufe in den Hintergrund; der Zug
endet dann vor dem Ergebnis. Was funktioniert: mehrere sequentielle
Vordergrund-Blöcke nach Testdateien, oder der Orchestrator führt sie selbst
aus. `tests/test_prerender.py` allein braucht ~12 Minuten.

**Turn-Limit einplanen.** Der Reglerumbau lief mit `--max-turns 250` ins Limit;
Code war fertig, aber nichts committet und nichts verifiziert. Bei großen
Aufträgen entweder aufteilen oder damit rechnen, die Verifikation selbst zu
übernehmen.
