# Kollektivtraum (Tool 2) — Einstieg für die neue Session

Kopiere den Abschnitt „PROMPT FÜR DIE NEUE SESSION" unten als erste Nachricht.
Alles davor ist Kontext zum Nachlesen, falls die neue Session tiefer graben muss.

---

## Was in der Vorsession passiert ist (2026-08-26)

Projekt: `~/projekte/kollektivgedaechtnis`, GitHub
`birkschmithuesen/kollektivgedaechtnis`.

> **🔴 Ab jetzt wird direkt auf `master` gearbeitet** (Birk, 2026-08-26).
> Der Feature-Branch `tool1-implementation` ist vollständig nach `master`
> gemerged und damit erledigt — er hatte historisch 113 Commits vor `master`
> gehalten, weil beide Tools dort entstanden sind. Das ist aufgelöst: `master`,
> `tool1-implementation` und beide `origin/*` stehen auf demselben Commit.
> **Keine neuen Feature-Branches anlegen, nicht auf `tool1-implementation`
> weiterarbeiten** — committen und pushen direkt auf `master`.

**Abgearbeitet:**

1. **Bild-Contract verifiziert** (`docs/dream-image-contract.md`) — war als
   „NOCH NICHT VERIFIZIERT" markiert, jetzt gegen den echten OpenRouter-
   Endpunkt geprüft. Zwei Abweichungen gefunden und dokumentiert: `images`
   enthält **zwei pixelidentische Einträge** (nur Metadaten unterscheiden sich,
   `images[0]` ist korrekt), und `message.content` ist `None` statt Text.
   Commit `85cce5b`.
2. **Kostenkorrektur:** Ein Bild kostet **0,139 USD**, nicht „ein paar Cent",
   wie Doku und Runbook behaupteten. Gemessen aus `usage.cost`.
3. **Kalibrierungsläufe gefahren:** Leitfrage (16 Sätze,
   `out/calibrate-questions.txt`), Widerspruchsschwelle
   (`out/calibrate-contradiction.txt`), 4 Register (`out/register1/`),
   3 fotorealistische Register (`out/register2-fotoreal/`),
   Kette-vs-scratch-Vergleich (`out/chain-vs-scratch/`).
4. **Streifen umgebaut:** `wrap` als Standard + neue Anzeige-Einstellung
   `strip_max` (Obergrenze, Default 10, Regler „Streifenlänge" im
   Operator-UI). Committet und gemerged.

**⚠️ `config2.example.toml` ist NICHT nachgezogen.** Birks Entscheidungen sind
dokumentiert, aber die Beispielconfig trägt weiterhin die alten Startwerte —
das gehört mit erledigt, sobald die jeweilige Aufgabe gelaufen ist:

| Schlüssel | steht auf | soll laut Birk |
|---|---|---|
| `guiding_question` | „Wie leben und bauen wir in zehn Jahren?" (Kandidat A) | **Kandidat B**: „Wie wollen wir in zehn Jahren zusammen wohnen und bauen?" — evtl. noch besser nach Punkt D |
| `visual_register` | malerisch-atmosphärisch, **„Kein Fotorealismus"** | **fotorealistisch/hyperreal, positiv** — also das genaue Gegenteil der aktuellen Zeile. Erst nach Punkt 7 (Register nachschärfen) setzen. |
| `contradiction_min_persons` | 6 | Klausel fällt ganz weg → Schlüssel entfernen oder als wirkungslos markieren (Punkt 3) |

Achtung: Es gibt einen Test, der prüft, dass der `guiding_question`-Wert aus
`config2.example.toml` **wörtlich** auch in `docs/operations.md` steht
(`test_the_guiding_question_default_in_the_runbook_matches_the_config`). Beim
Ändern beide Stellen mitziehen.

**Zwei Umgebungsfunde, die Zeit sparen:**

- `~/.hermes/.env` hat **`ANTHROPIC_API_KEY=` ohne Wert** (Birks Hermes fährt
  Claude übers Abo per Proxy). Stufe-1-Läufe auf dem vServer brauchen deshalb
  zusätzlich: `export ANTHROPIC_BASE_URL=http://127.0.0.1:28764` und
  `export ANTHROPIC_API_KEY=proxy`. Ohne das läuft `sim.dream_calibrate`
  **lautlos durch** und druckt nur FEHLER. `OPENROUTER_API_KEY` ist echt.
- Volle Testsuite dauert **~23 Minuten** (Playwright). Bei Delegation an
  Claude Code `--max-turns` großzügig setzen (120+), sonst stirbt der Lauf an
  `error_max_turns`, bevor committet wird — genau das ist einmal passiert.

---

## 🔴 Offene Baustellen (zuerst erledigen)

### 1. Git-Stand — erledigt, ab jetzt direkt auf `master`

**Alles ist committet, nach `master` gemerged und gepusht.** `master`,
`tool1-implementation` und beide `origin/*` stehen auf demselben Commit, das
Arbeitsverzeichnis ist sauber und **ausgecheckt ist `master`**. Die zweite
Session (Tool 1, „Dichte im Operator-UI") hat ihre Arbeit ebenfalls gemerged
(`3954859`) und ist fertig. **Es gibt nichts nachzuholen.**

**Birks Vorgabe für die Folgesession: direkt auf `master` arbeiten.** Keine
neuen Feature-Branches, `tool1-implementation` ist Geschichte. Auto-Commit und
Auto-Push sind erlaubt (Birks stehende Regel) — also committen und pushen,
ohne zu fragen.

Die Lehre für den Fall, dass doch wieder parallel gearbeitet wird: Der
Git-Index ist zwischen Sessions **geteilt**, `git add <liste>` + `git commit`
ist NICHT atomar. In der Vorsession hat das dazu geführt, dass ein Commit
versehentlich die **fremde** Tool-1-Arbeit unter einer Tool-2-Message enthielt
(mit `git reset --soft HEAD~1` zurückgebaut, nicht gepusht, kein Schaden).
**Regel: `git commit -F <msgfile> -- <pfade>`, danach `git show <sha> --stat`
gegenprüfen.** (`-m`/`-F` müssen vor `--` stehen, sonst frisst git die Message
als Pfad.)

### 2. 🔴 JPEG-Bug — der kritischste offene Punkt

`google/gemini-3-pro-image` liefert **pro Aufruf mal PNG, mal JPEG**
(deklariert als `data:image/jpeg;base64,`). `kg2/imagegen.py::save_image`
prüft hart auf die PNG-Magic-Number und wirft sonst `ImageError` → Traum
`failed`.

**Gemessen: 2 von 5 Bildern kamen als JPEG (~40 %).** Am Ausstellungstag wäre
so rund jedes zweite Bild verworfen worden.

Fertiger Auftragsbrief liegt in `/tmp/kg-jpeg-brief.md` (falls weg: neu
schreiben). Kern: PNG **und** JPEG akzeptieren, Dateiendung aus dem erkannten
Format ableiten, prüfen wer den Pfad liest (`kg2/cycle.py`, Frontend,
`dreams.sqlite3`), Überschreibschutz und `ImageError`-Verhalten unverändert
lassen. Contract-Dokument zuerst korrigieren, dann Code (das Dokument ist die
Autorität, steht so drin).

---

## Birks Entscheidungen aus der Vorsession (bindend)

| Frage | Entscheidung | Birks Begründung |
|---|---|---|
| Streifenmodus | **`wrap`** | `cover` schneidet Bilder zu |
| Streifenlänge | **Default 10**, im Operator-UI steuerbar | „40 Träume sind zu viel" |
| Bild-Erzeugung | **from scratch**, NICHT Kette | „Kette sind mir die Bilder doch zu ähnlich." Zusatzargument: Schon die Sätze bei 3/10/30/60 Personen ähneln sich stark — als Kette wäre „kaum Veränderung drin" |
| Register-Richtung | **hyperreal** von den dreien | „unheimlich muss nicht unbedingt sein" |
| **Bildstimmung** | **positiver!** | „die Bilder wirken eher runtergekommen, sie sollen positiver sein" |
| Leitfrage | **Kandidat B** — „Wie wollen wir in zehn Jahren zusammen wohnen und bauen?" | „der soziale Miteinander-Aspekt ist wichtig" |
| Widerspruchs-Klausel | **weglassen** | „zwingt ein ‚aber/oder' rein, das vermutlich Halluzination provoziert". Auch ohne Klausel seien die Sätze „schon schön absurd" |
| Begriffsauswahl | **nur Begriffe mit ≥ 2 Nennungen** | „nicht alle Begriffe nehmen!" |
| Modellwechsel | **jetzt nicht** | „lass lieber erst mit dem aktuellen Modell alles fertig machen und dann nochmal schauen, ob wir das billiger bekommen" |
| Kosten | 0,70 USD für 5 Bilder findet er **teuer** | daher die Preisrecherche unten |

**Arbeitshaltung, die Birk mitgegeben hat:** „müssen da jetzt aber auch nicht
endlos basteln" — pragmatisch entscheiden statt perfektionieren. Er hatte
gefragt, ob sich beim Ketten-Verfahren einstellen lässt, **wie viel vom
Originalbild übernommen wird** (Stärke der Bearbeitung), hat die Frage aber
selbst zugunsten von from-scratch zurückgestellt. Falls das Thema
wiederkommt: über die Prompt-Formulierung steuerbar, ein echter
„denoising strength"-Parameter ist bei diesem Endpunkt nicht dokumentiert.

**Wichtig zum Stil:** Register muss insgesamt nachgeschärft werden — Richtung
hyperreal **und positiver**. Die bisherigen Muster in `out/register2-fotoreal/`
treffen den Ton noch nicht.

---

## Birks neue Aufgaben (das ist die eigentliche Agenda)

### A. Mood-Analyse auf dem Graphen

Neue Idee: Vor der Bildgenerierung soll analysiert werden, **ob die
Zukunftsstimmung im Material gerade eher positiv oder negativ ist**, und das
soll in den Bildstil einfließen. Also nicht ein fixes Register für den ganzen
Tag, sondern ein Register + eine stimmungsabhängige Komponente.

⚠️ **Konflikt, der geklärt werden muss:** Spec §10 legt ausdrücklich
**ein festes Register für den ganzen Tag** fest, mit der Begründung, der
Verlaufsstreifen sei eine Messreihe und dürfe **nur eine Variable** haben
(das Material). Ein wanderndes Register lässt den Streifen Stiländerungen
zeigen statt inhaltlicher Drift. Spec §10 verwirft „graph-driven style"
zusätzlich als „unprovable in the room".
→ Das ist eine bewusste Design-Entscheidung, die Birks neue Idee direkt
berührt. **Nicht stillschweigend überschreiben** — Birk vorlegen: entweder
Spec §10 bewusst ändern, oder die Stimmung nur begrenzt einfließen lassen
(z. B. Licht/Farbtemperatur statt ganzem Register).

### B. Begriffsauswahl ändern (`kg2/weighting.py`)

Birk: **nur Begriffe nehmen, die mindestens 2× genannt wurden** — nicht alle.

Aktueller Stand im Code: `build_material()` teilt in `shared` (≥2 Nennungen)
und `marginal` (genau 1), und `render_material()` gibt **beide** in den Prompt,
die marginalen als „Randnotizen … Detail und Beiwerk". Das Modul-Docstring
sagt ausdrücklich: „**`min_mentions` is NOT applied**. That dial is the wall's
legibility filter, not a statement about what was said."
→ Birks Wunsch kehrt diese Entscheidung um. Umsetzen, Docstring und Doku
mitziehen, Begründung sauber ersetzen statt widersprüchlich stehen lassen.

### C. Reihenfolge-Einfluss prüfen + Shuffle

Birk fiel auf, dass die Sätze bei 60 Personen/163 Begriffen den Sätzen bei
30/93 **erstaunlich ähnlich** sind. Auftrag: herausfinden, ob die
**zuerst genannten Begriffe stärkeren Einfluss** haben (aktuell sortiert
`build_material` streng nach Häufigkeit absteigend, dann alphabetisch — also
immer dieselbe Reihenfolge). Danach: **bei jeder neuen Analyse die Begriffe
shuffeln**.
⚠️ Achtung: Die feste Sortierung ist absichtlich („zwei Läufe über denselben
Graphen müssen denselben Prompt erzeugen, sonst erklärt der Datensatz in
spec §5.3 nichts"). Ein Shuffle muss also **mit Seed** laufen und der Seed
muss mit im Datensatz landen, sonst ist die Reproduzierbarkeit weg.

### D. Leitfrage gegen das Konferenzprogramm prüfen

Birk hat Kandidat B gewählt, will aber, dass das Programm der Konferenz
(NEW bauhaus 2026) durchgegangen wird: Gibt es eine **bessere Frage, die in
den meisten Vorträgen als Fragestellung steckt**? Programmpunkte suchen und
gegen die vier Kandidaten halten.

### E. Kette Stufe 1 → Stufe 2 sauber beschreiben

Birks Frage: **Wie ist die Übergabe von der Leitfragen-Antwort zum Bildprompt?**
Der Bildprompt muss noch mit Ästhetik/Style/Register/Stimmung angereichert
werden. Und: **Wie wird der Satz destilliert, der unter dem Bild steht?**

Stand im Code (für die Antwort):
- **Stufe 1** (`kg2/condense.py`): `build_condense_system()` baut den
  System-Prompt aus `_BASE` (+ optional `_CONTRADICTION` — soll laut Birk
  weg), `build_condense_prompt()` den User-Teil aus `render_material()`.
  Ergebnis ist **ein deutscher Satz** (`DreamSentence`), der **direkt auf der
  Wand steht**. Es gibt aktuell **keine** zweite Destillationsstufe.
- **Stufe 2** (`kg2/imagegen.py`): `build_image_prompt(sentence, register,
  aspect_ratio)` hängt Register + Format **hinter** den Satz. Reihenfolge ist
  begründet: Satz zuerst = Motiv, Register danach = Machart.
- Der Bildprompt wird bewusst **nicht** angezeigt (Spec §5.2) — sonst stünden
  Lichtanweisungen auf der Wand.

### E2. 🔴 Der Satz ist VIEL zu lang für eine Bildunterschrift

**Birks Befund am generierten Material (2026-08-26):** „das was du mir gezeigt
hast waren eher so 3-5 Sätze und viel zu viele Wörter um sie als ein Satz unter
das Bild zu setzen. Das geht so nicht."

Nachgemessen über alle 16 Sätze aus `out/calibrate-questions.txt`:

| | |
|---|---|
| Wörter | min 30, max 50, **Schnitt 37** |
| Teilsatz-Marker (Komma/Semikolon/Gedankenstrich/„während"/„und") | **5 bis 8 pro Satz** |
| über der eigenen Zielvorgabe (40) | 2 von 16 |

Formal ist es **ein** Satz — die Vorgabe „genau ein Satz" wird eingehalten.
Aber mit 37 Wörtern und 5–8 Teilsätzen liest er sich wie drei bis fünf. Die
Zielvorgabe im Code ist selbst zu großzügig:

```python
# kg2/condense.py
WORDS_MIN, WORDS_MAX = 20, 40
```

Das Modell landet erwartbar am oberen Rand und schachtelt Nebensätze. Spec §5.1
begründet die Spanne mit „long enough to carry the fault line, short enough to
read at a glance from standing distance" — der zweite Teil ist am realen
Material widerlegt.

**Auftrag an die neue Session: einen VORSCHLAG machen** (nicht einfach eine
Zahl setzen), der mindestens beantwortet:

1. **Welche Länge** ist aus Stehdistanz auf einem 65-Zoll-Schirm lesbar? Nicht
   raten — das lässt sich am Frontend messen (`frontend2/dream.html`,
   Schriftgröße, Streifenhöhe), analog zur bereits gemessenen Lesbarkeitsreihe
   bei Tool 1 (`docs/operations.md`: „1× ergibt ~13 px Schrift auf 1920 Pixel
   Wandbreite").
2. **Nur Wortzahl senken, oder Schachtelung begrenzen?** Eine reine
   Wortgrenze verhindert nicht, dass 25 Wörter aus vier Teilsätzen bestehen.
   Denkbar: zusätzlich die Zahl der Teilsätze/Kommas begrenzen.
3. **Eine Stufe oder zwei?** Der Satz hat zwei Aufgaben, die sich beißen: Er
   ist (a) Bildunterschrift auf der Wand und (b) Motivbeschreibung für Stufe 2.
   Für (b) ist Länge und Detail nützlich, für (a) schädlich. Option wäre ein
   langer Bildprompt-Satz plus eine kurze Wandzeile — das wäre allerdings eine
   echte Architekturänderung (Spec §5 begründet die Zweistufigkeit gerade
   damit, dass der ANGEZEIGTE Satz derselbe ist, den das Bild bekam). Vorlegen,
   nicht einfach bauen.
4. Vorschlag **am echten Material zeigen**: ein paar Sätze in der neuen Länge
   generieren und Birk gegenüberstellen, nicht nur beschreiben.

Wenn das geklärt ist, will Birk **mit echten Sätzen/Analysen ein paar
Bildgenerierungen** laufen lassen.

### F. Register nachschärfen — Prompt-Design (Birks Kernanliegen)

**Ausgangslage:** Birk hat aus vier malerischen Registern keines genommen,
sondern die Richtung gewechselt. Seine Worte:

> „der Style soll fotorealistisch sein, aber nicht ein Foto einer realistischen
> Zukunft, sondern es kann sehr absurd und unrealistisch sein. So hatte ich die
> Idee mit einer nicht plausiblen, widersprüchlichen Bild gemeint. Also ein
> absurdes Foto, weirde Film Stills."

Und nach dem zweiten Durchgang (`out/register2-fotoreal/`):

> „hyperreal finde ich am besten. unheimlich muss nicht unbedingt sein."
> „die Bilder wirken eher runtergekommen, sie sollen positiver sein."

**Warum das KEIN Spec-Bruch ist** (wichtig, damit die neue Session nicht
fälschlich blockiert): Spec §1 verbietet „a plausible architectural vision" —
also die **glatte, plausible Zukunftsdarstellung**, nicht Fotorealismus als
Machart. Register und Motiv sind zwei Ebenen: fotoreal ist der **Stil**, die
Unmöglichkeit sitzt im **Inhalt**. Ein absurdes Filmstill erfüllt §1 sogar
besser als ein Aquarell, weil das Malerische die Absurdität weichzeichnet,
während das Fotorealistische sie hart macht. Die Klausel „Kein Fotorealismus"
stammt aus den in der Vorsession selbst geschriebenen Registerkandidaten
(`sim/dream_register.py`), **nicht** aus der Spec — sie ist hinfällig.

**Der offene Widerspruch, den die neue Session lösen muss:** „hyperreal" und
„positiv" ziehen gegeneinander. Das bisher beste Muster
(`register-unheimlich-hyperreal.png`) erkauft seine Schärfe mit „kühles
diffuses Licht, unnatürlich still, zu perfekte Oberflächen" — genau das macht
es unheimlich und runtergekommen. Gesucht ist **hyperreale Schärfe ohne den
kalten/verfallenden Ton**. Ansatzpunkte, die im Prompt zu drehen sind:

- **Licht**: kühl/diffus → warm, klar, Tageslicht statt Nebel
- **Zustand der Dinge**: bröckelnd/verfallen/leer → intakt, benutzt, belebt
- **Farbe**: gedämpft/entsättigt → satt, nicht grell
- **Menschen**: die bisherigen Bilder wirken menschenleer; Anwesenheit macht
  ein Bild sofort weniger dystopisch
- Das Absurde muss **bleiben** — positiv heißt nicht harmlos oder schön.
  Ein freundlich belichtetes unmögliches Bild ist das Ziel, kein Werbebild.

⚠️ Ein Teil des düsteren Tons kommt gar nicht aus dem Register, sondern aus dem
**Material**: Die Testsätze sind voller „bröckelnder Putz", „sozial tote
Tiefgarage", „leerer Dorfkern". Das ist ein Grund mehr, den Zusammenhang mit
Punkt A (Mood) zu klären — und beim Registertest zu prüfen, ob ein neutralerer
Satz schon reicht, um die Bilder freundlicher zu machen.

**Vorgehen:** Varianten rendern (je ≈0,14 USD), als beschriftete Kontaktkarte
in den Chat legen, Birk entscheidet an Bildern. Gewählten Registertext danach
als `visual_register` in `config2.example.toml` eintragen (dort steht aktuell
noch das Gegenteil, s. o.).

**Werkzeug:** `sim/dream_register.py` hat mit
`render_samples(out_dir, cfg, registers={...})` einen Injektionspunkt — eigene
Registerkandidaten lassen sich durchreichen, ohne die Datei zu ändern. Die
Vorsession hat das so gemacht (`/tmp/kg_register_fotoreal.py` als Vorlage).

**Automatische Kontrolle, die sich bewährt hat:** Jedes Register verlangt
„keine Schrift im Bild". Ein OCR-Durchlauf über die Muster (RapidOCR) findet
Verstöße — im ersten Durchgang hatte „Siebdruck" schriftähnliche Zeichen
(`三上`). Lohnt sich vor jeder Registerwahl.

### G. Bildprompt-Aufbau (Birks Frage nach der Anreicherung)

Birk: „der Bildprompt muss dann ja noch mit Ästhetik, Style, Register,
positiv/negativ etc. angereichert werden."

Aktuell (`kg2/imagegen.py::build_image_prompt`) ist der Aufbau simpel:

```
<Satz aus Stufe 1>

Bildsprache (unveränderlich, gilt für jedes Bild dieser Reihe): <visual_register>
Format: Seitenverhältnis <aspect_ratio>, Querformat.
```

Also genau zwei Bausteine: Motiv + Register. Wenn die Mood-Komponente (Punkt A)
dazukommt, ist **hier** die Stelle, an der sie eingehängt wird — und die neue
Session sollte Birk den zusammengesetzten Prompt konkret zeigen, bevor sie ihn
baut. Die Reihenfolge (Satz zuerst) ist begründet und sollte bleiben: „a prompt
that opens with lighting instructions gets an image about lighting."

---

## Modellpreise — Kurzeinschätzung (Birk wollte nur grob)

Abfrage des OpenRouter-Katalogs: 11 Modelle mit Bild-Ausgabe. Abgerechnet wird
**pro Output-Token**, nicht pauschal pro Bild; ein Bild kostet bei uns ~1120
Bild-Tokens + Reasoning ≈ 1480 Tokens.

| Modell | USD/Mtok out | grob USD/Bild |
|---|---|---|
| `google/gemini-3-pro-image` (aktuell) | 12,00 | **0,139** (gemessen) |
| `google/gemini-3.1-flash-image` | 3,00 | ~0,035 |
| `google/gemini-3.1-flash-lite-image` | 1,50 | ~0,017 |
| `google/gemini-2.5-flash-image` | 2,50 | ~0,03 |
| `openai/gpt-5-image-mini` | 2,00 | ~0,023 |
| `openai/gpt-5-image` / `gpt-5.4-image-2` | 10,00 / 15,00 | ~0,12 / ~0,17 |

**Einschätzung:** Die Flash-Varianten liegen bei **4–8× günstiger**. Ein voller
Ausstellungstag (~40 Bilder) fiele von ~5,55 USD auf **0,7–1,4 USD**. Bei diesen
absoluten Beträgen ist der Sparbetrag allerdings klein gegen den Aufwand: Ein
Modellwechsel bedeutet **Register neu fine-tunen** (anderer Look), und die
Bildqualität ist bei Flash-Modellen typischerweise schwächer. Birks eigene
Priorisierung — erst mit dem aktuellen Modell fertig werden, danach Preisfrage
neu stellen — ist die richtige Reihenfolge. Ungeprüft ist, ob die Flash-Modelle
`modalities: ["image","text"]` und img2img identisch bedienen.

---

## Nützliche Fakten

- **img2img funktioniert** mit dem aktuellen Modell (am echten Endpunkt
  verifiziert, OpenAI-Multimodal-Shape mit `image_url`-Part). Wird nach Birks
  Entscheidung erstmal nicht gebraucht, aber der Weg ist bekannt.
- **Kein `sim.dream_prerender --generate` starten** ohne Birks OK — 40 Bilder
  ≈ 5,55 USD.
- Vergleichsskripte der Vorsession liegen unter `/tmp/kg_*.py` (throwaway,
  nicht im Repo): `kg_chain_vs_scratch.py`, `kg_register_fotoreal.py`,
  `kg_chain_probe.py`, `kg_format_probe.py`, `kg_model_prices.py`.
- Birk entscheidet **an Bildern**, nicht an Beschreibungen. Kontaktkarten
  bauen (mehrere Varianten nebeneinander, beschriftet) und in den Chat legen.
- Die Doku sagt an mehreren Stellen ausdrücklich: **keine Empfehlung** im
  Output der Kalibrier-Werkzeuge. Das ist Absicht.

---

## PROMPT FÜR DIE NEUE SESSION

> Wir arbeiten am Kollektivtraum (Tool 2) in `~/projekte/kollektivgedaechtnis`.
> Lies zuerst `docs/HANDOFF-2026-08-26.md` **vollständig** — dort stehen der
> Stand, meine Entscheidungen mit Begründungen und alle offenen Punkte (A–G).
> Danach `docs/operations.md` (Abschnitt „Kollektivtraum — Screen B") und die
> Spec `docs/superpowers/specs/2026-08-25-kollektivtraum-brainstorm.md`
> (§1, §5, §10).
>
> **Arbeite die Punkte A–G aus dem Handoff ab — keiner darf unter den Tisch
> fallen.** Wo im Handoff steht „Birk vorlegen", bau nicht einfach los, sondern
> frag mich. Sag mir zu Beginn kurz, welche Punkte du siehst und in welcher
> Reihenfolge du sie angehst.
>
> **Git:** direkt auf `master`, keine Feature-Branches. Alles ist committet und
> gepusht, Arbeitsverzeichnis sauber. Commit und Push ohne Rückfrage.
>
> Reihenfolge:
> 1. **JPEG-Bug** fixen — das Modell liefert mal PNG, mal JPEG, gemessen 2 von 5;
>    aktuell wird jedes JPEG als „Traum failed" verworfen. (Handoff Punkt 2)
> 2. **Begriffsauswahl** umstellen: nur Begriffe mit mindestens 2 Nennungen
>    (kehrt eine bisherige Design-Entscheidung in `kg2/weighting.py` um —
>    Docstring und Doku mitziehen). (Punkt B)
> 3. **Widerspruchs-Klausel entfernen** und stattdessen im Prompt erzwingen, dass
>    alles an den Begriffen belegbar ist und nicht halluziniert wird. (Punkt B)
> 4. **Satzlänge:** Der Satz ist als Bildunterschrift unbrauchbar — im Schnitt
>    37 Wörter mit 5–8 Teilsätzen, liest sich wie 3–5 Sätze. Mach mir einen
>    **Vorschlag** und zeig ihn an echten Sätzen, nicht als Beschreibung.
>    (Punkt E2)
> 5. **Reihenfolge-Einfluss** prüfen, danach **Shuffle mit Seed** (Seed muss in
>    den Datensatz, sonst ist die Reproduzierbarkeit aus Spec §5.3 hin). (Punkt C)
> 6. **Erklär mir die Kette** Leitfrage → Satz → Bildprompt und wo Ästhetik,
>    Register und Stimmung angereichert werden. Zeig mir einen fertig
>    zusammengesetzten Bildprompt. (Punkte E + G)
> 7. **Register nachschärfen:** hyperreal, aber **positiver** — die bisherigen
>    Bilder wirken runtergekommen. „unheimlich" muss nicht sein. Achtung, das
>    zieht gegeneinander; im Handoff Punkt F stehen die Stellschrauben (Licht,
>    Zustand, Farbe, Menschen). Varianten rendern, als Kontaktkarte zeigen —
>    ich entscheide an Bildern.
> 8. **Leitfrage:** ich habe Kandidat B gewählt. Geh trotzdem das Programm der
>    NEW bauhaus 2026 durch, ob eine Frage besser passt, die in den meisten
>    Vorträgen als Fragestellung steckt. (Punkt D)
> 9. **`config2.example.toml` nachziehen** — dort stehen noch die alten Werte,
>    beim `visual_register` sogar „Kein Fotorealismus", also das Gegenteil
>    meiner Entscheidung. Jeweils nach dem passenden Schritt aktualisieren.
>
> **Zwei Dinge vorlegen statt bauen:**
> - **Mood-Idee** (Stimmungsanalyse auf dem Graphen, die in den Bildstil
>   einfließt): kollidiert mit Spec §10 (ein festes Register den ganzen Tag,
>   damit der Verlaufsstreifen nur eine Variable hat). (Punkt A)
> - **Satzlänge ein- oder zweistufig** (Punkt E2, Frage 3) — das wäre eine
>   Architekturänderung.
>
> Keine 40-Bilder-Serie ohne mein OK, die kostet ~5,55 USD. Einzelne
> Testbilder (~0,14 USD) kannst du ohne Rückfrage rendern.
