Seventh round — SHORT correction. Do not touch plan Tasks 18/19.

## What is wrong

The prerender6 legibility ladder is unusable at 32px and 44px: labels overlap AND the person discs overlap each other. Birk cannot judge type size from a broken picture.

Root cause is my previous brief, not your implementation: I asked for "same placement across all three" so that only type size would differ. That instruction is self-contradictory. Larger labels need more room, so the net MUST spread further; a layout computed for 22px labels cannot carry 44px ones. Person discs also scale with the variant (30px → 63px) while the spacing did not.

## The fix

Re-render the legibility ladder into out/prerender7/ with EACH VARIANT LAID OUT FOR ITSELF:
- Every variant runs its own full layout (fcose + settlePlacement + declutterLabels) with ITS OWN label sizes and ITS OWN disc sizes fed into the layout as the collision extents. `nodeDimensionsIncludeLabels` must see the variant's real type size, not A's.
- Success criterion, measured and reported per variant: **0 overlapping label pairs, 0 labels on discs, and 0 disc-on-disc overlaps.** Disc-on-disc is a new check — add it, it was never measured before and it is now a stated defect.
- If a variant cannot reach 0/0/0 at the target fill, do NOT silently ship it: report the residual numbers and say what limits it (this is legitimate — 44px type on 75 terms may simply not fit at readable density, and that is a real finding Birk needs).

Report per variant: model type size, effective px on a 1920-wide wall, zoom, canvas fill %, and the three overlap counts before/after declutter.

Same seed, same graph, pure black/white palette as prerender6, 1920x1080.

The comparison is still valid: it now answers "at this type size, this is how dense the net can be while staying clean" — which is the question that actually matters for the wall, and better than three pictures at one fixed geometry.

Deliver the three PNGs plus absolute paths. Then stop. Implementers on sonnet/haiku.
