"""Pre-render harness for Tool 1's live projection (spec §10.3/§10.4).

Not the simulation replay engine (Tasks 18/19, not built yet) — see the
Task 20 brief for why `sim.seed_graph` writes state directly through
`kg.store.Store` instead.
"""
