Birk reviewed the pre-render series. Four changes — this is a new pre-render iteration, NOT the remaining plan tasks. Do not start Tasks 18/19.

1. WHITE ON BLACK STAYS. Theme B (inverted, light ground) is REJECTED — drop it from the series. Sync that decision into spec section 10.4 and the plan: the comparison series no longer contains an inverted variant. Replace B's slot with a legibility variant instead (see point 3).

2. USE THE FULL 16:9 WIDTH. The graph currently occupies only ~55% of 1920px because the force layout is radial and therefore circular, which wastes the sides on a 16:9 canvas. Fix this in the LAYOUT, not by over-zooming the camera (zooming further clips top and bottom). Options: give the force layout a horizontal bias / aspect factor, post-scale the settled positions on x so the point cloud is 16:9-shaped, or use a layout with an explicit boundingBox matching 1920x1080. Whatever you choose must stay deterministic under the same seed. Verify by measuring the rendered bounding box of all nodes as a fraction of canvas width — report the before/after numbers.

3. BIGGER TYPE, HIGHER LEGIBILITY. Birk cannot read the labels. Current --label-size is 22px on a 1080-line canvas with 75 terms. Produce a series that varies exactly this: label size and stroke/outline weight, all on dark ground. Suggested variants (rename freely, but keep them all dark): base (current), larger type (~32px), much larger type (~44px) with heavier node/edge strokes and a stronger text outline. Node sizes and edge widths must scale sensibly with the type so the picture stays balanced.

4. ZOOM PROPOSAL RENDERINGS. Birk wants to SEE what zoomed-in operation looks like, since fit-all at 50 persons is now settled as illegible. Render a set showing the camera at meaningful zoom levels over the SAME graph: fit-all (reference), a moderate zoom showing roughly half the graph, and a close zoom showing a small cluster of a few persons with their terms — the kind of view the auto-pan mode would traverse. Use the existing camera component's modes; do not invent a parallel implementation.

Deliver ALL PNGs at exactly 1920x1080 into out/prerender2/ with self-explanatory filenames, and print the absolute paths plus a one-line description of each. Keep everything deterministic and regenerable via the CLI; update the CLI flags if needed.

Then stop and report. Keep implementers on sonnet/haiku; opus only for hard review.
