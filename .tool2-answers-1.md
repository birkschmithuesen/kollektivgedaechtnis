Answers to all six. Where I confirm your proposal, take it as decided and do not re-open it.

**1. Contract fixture — yes, your proposal, with one addition.**
Commit `sim/data/graph-19c.json` with the provenance header. The drift guard is
exactly right and is the more important half: a fixture that silently stops
matching Tool 1's real export is worse than no fixture. Do NOT make contract
tests invoke a fresh replay run — that needs credentials and minutes, and it
would make Tool 2's suite unrunnable offline, which Tool 1 deliberately avoided
(its embedding cache exists for that reason). Addition: the drift guard asserts
the *key sets and types*, not the values, so legitimate content change doesn't
produce red tests.

**2. Correct, and this is a defect in my spec — fix the spec, don't work around it.**
You are right: `server_host` is already a Config field, already in
`_FIELD_NAMES`, already passed to uvicorn (`kg/__main__.py:50`). Spec §3.1 claims
it "must become bindable". That is wrong and must not be planned as
implementation work.

- Amend `docs/superpowers/specs/2026-08-25-kollektivtraum-design.md` §3.1 to say
  the mechanism already exists and the remaining work is configuration,
  documentation and a cross-machine verification. Same for the sentence in
  Tool 1's spec §12 that says the change "lands in this tool". Note the
  correction with the date — the spec is a decision record, so a wrong claim
  gets corrected visibly, not quietly deleted.
- Yes to the narrow task: `docs/operations.md` bind value + the cross-machine
  check + a `load_config` round-trip test. Nothing else.
- Yes to the print fix, and it is not cosmetic: a runbook that tells the
  operator to open a URL that cannot work from the other box is a trap on the
  festival morning. Print the resolved LAN address when `server_host` is
  `0.0.0.0`, keep printing the literal host otherwise. That is the ONLY change
  inside `kg/` beyond documentation, and the task must say so explicitly.

**3. Yes, plan it like the register, and the plan proposes the wordings.**
You propose 3–4 candidates anchored on „Wie leben und bauen wir in zehn Jahren?".
Requirement from the spec (§10, brainstorm §7): each candidate must be wide
enough to carry all three interview themes — future of building, AI in building,
new forms of living together. A candidate that only fits one is not a candidate.
Run stage 1 at 3 / 10 / 30 / 60 persons per wording and print the sentences.
**Birk picks. Do not pick for him, and do not mark one as recommended in the
output** — the whole point is that he reads the sentences cold.

**4. Confirmed — separate `config2.example.toml` + `kg2/config.py`.**
Your reasoning is the right one: it runs on a different machine, so a shared
file would be a lie. Add `tool1_url` as you listed. One more field: the poll
interval (spec §4.1 says 5 s) belongs in config too, so the venue can be tuned
without a code change.

**5. Yes — probe first, build against the verified contract.**
Do it exactly like Task 3 did with the STT server, and write
`docs/dream-image-contract.md`. One real API call up front is trivially worth it;
building an image client against an assumed request shape is how you discover the
mismatch on site. Same rule as Tool 1's STT contract: verify against the real
endpoint, record what it actually returns, then code against the record. If the
probe reveals the model id or the response shape differs from what the spec
assumes, that is a finding to write down, not to paper over.

**6. Plan (a) with the (b) flag — but the execution order is (b) first.**
Your analysis is right: the strip at 40 is judged on variety, so faked variety
judges nothing. So plan (a) as the real path and keep the flag.

For the build itself, run the cheap path first: get the pre-render pipeline
working end-to-end on the ~6-image pool, so that when the 40 real images are
generated they are spent on a page that is already correct rather than on
debugging the harness. The full 40-image run is the last step before Birk's
visual review. Flag its cost in the task text so it is a visible, deliberate
spend and not a surprise line item.

---

Now write the plan to `docs/superpowers/plans/2026-08-25-kollektivtraum-tool2.md`,
following Tool 1's plan conventions. Include the spec amendment from answer 2 as
an early task.

Then continue straight into implementation using
superpowers:subagent-driven-development — do not stop and wait for approval
between the plan and the execution. Work through the tasks, keep the ledger at
.superpowers/sdd/progress.md, commit per task as Tool 1 did.

Two standing rules for the whole run:
- Where a task needs an aesthetic judgement (visual register, guiding-question
  wording), BUILD the artefacts and leave the choice open for Birk. Never pick.
- If you hit something that contradicts the spec, treat it like answer 2: the
  spec is wrong and gets corrected with a dated note. Do not silently build
  around a spec defect.
