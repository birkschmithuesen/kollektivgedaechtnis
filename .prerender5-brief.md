Fifth pre-render round — SHORT and focused. Do not touch the remaining plan tasks (18/19).

Birk's complaint is fair: the migration was delivered as 4 stills. Four frames cannot show a glide — played back they look like exactly the jumping we are trying to disprove. He wants to SEE the motion.

## DELIVER FRAME SEQUENCES DENSE ENOUGH TO BECOME VIDEO

Render at 25 fps over the full animation, into out/prerender5/, 1920x1080, theme B. Frame files must be zero-padded and sortable (e.g. `seq-<name>/frame-0001.png` ...). Put each sequence in its OWN subdirectory so it can be globbed cleanly.

Three sequences, each covering the whole transition plus ~0.5s settled tail:

1. `seq-dial-1-to-2/` — density dial raised from min_mentions 1 to 2 (25 terms vanish, the rest migrate outward into the freed space).
2. `seq-dial-2-to-1/` — the reverse: hidden terms return and everyone makes room. This is the harder direction and the one that used to re-shuffle, so it is worth proving.
3. `seq-new-person/` — one new person node with its terms joining a settled ~30-person graph. This is the transition the audience will actually see most often, and spec §6 says the person node appears immediately on the photo trigger.

Use the wall's REAL default glide duration (~2.5s), not the slowed 8s from last round — Birk needs to judge the actual speed, and at 25fps a 2.5s glide is ~63 frames, which is plenty.

Also encode each sequence to an MP4 yourself if you can (H.264, yuv420p, 25fps, so it plays inline on Telegram); if no encoder is available in the venv, just deliver the frames and say so — I will encode. A working ffmpeg binary exists at:
/home/birk/.local/lib/python3.9/site-packages/imageio_ffmpeg/binaries/ffmpeg-linux-x86_64-v7.0.2

Determinism still applies: same seed, same frames.

Report the absolute path of each sequence directory (and MP4 if produced), the frame count, and the measured wall-clock duration each sequence represents.

Also stop. Implementers on sonnet/haiku.

## 🔴 COLOUR CORRECTION — apply BEFORE rendering anything (Birk, 2026-08-15)

The background must be **pure black `#000000`** and the label text **pure white `#FFFFFF`**. Currently theme B uses `--bg: #101014` (a blue-tinted near-black) and `--label-color: #F5F1E6` (a cream near-white). Birk wants maximum contrast, no tint.

Change this in ALL graph themes (a, b, c), not just B:
- `--bg` → `#000000`
- `--label-color` → `#FFFFFF`
- `--label-outline-color` → `#000000` (must match the new background, or the outline shows as a halo)
- Re-check every other token against the new extremes: `--term-dot-color` (currently `#EDE7D8`) should become pure white too unless there is a reason it must differ from the labels; `--edge-color` (`#8A8578`) is a grey that was tuned against the old tinted background — restate it as a neutral grey, keeping its relative luminance so edges stay subordinate to the labels. The golden ring `--ring-color: #C9A227` STAYS gold — it is the concept's signature and is not a greyscale element.

Rationale to record in the spec: projection is additive and the surface is a whiteboard, so on-site black is whatever ambient light sits on it (spec §10.4). A tinted near-black gains nothing there and costs contrast; pure values give the projector the maximum range it can deliver.

Sync the new values into spec §10.4 / the plan's theme CSS blocks, and mention it in the ledger. Then render the sequences below with the corrected themes.

