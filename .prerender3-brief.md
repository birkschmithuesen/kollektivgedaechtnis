Third pre-render iteration from Birk's review of series 2. NOT the remaining plan tasks — do not start Tasks 18/19. Deliver into out/prerender3/, all PNGs exactly 1920x1080, deterministic, regenerable via the CLI.

BASELINE: variant B (32px labels) is Birk's choice and becomes the default theme for every rendering below. Final judgement happens on a real projector on site, so keep A (22px) and C (44px) regenerable, but render this round on B.

1. PLACEHOLDER PORTRAITS: ONE COLOUR, SMALLER. The multicoloured discs fight the text and are misleading (the colours mean nothing). Make all placeholder portraits a single uniform muted colour and noticeably smaller than now. Keep the golden ring — it is the concept's carrier. Real photographs later will bring their own structure, so the placeholder must not pretend to be information.

2. LABEL OVERLAP — three structural fixes, in this order:
   a) LAYOUT MUST KNOW THE LABELS. Right now the force layout only separates the dots, but a term node is visually the dot PLUS its text block, and "Genossenschaftliches Wohnen" is ~10x wider than its dot. Feed the real rendered extent (dot + label box) into the layout as the collision/repulsion shape — e.g. Cytoscape bounding-box compound sizing, per-node width/height derived from measured text, or equivalent. This is the highest-leverage change.
   b) POST-LAYOUT LABEL DECLUTTER. After the layout settles, run a collision pass that nudges only the LABEL offsets (not the node positions) by a few pixels until no two label boxes overlap. Node positions must stay put — position persistence (spec §11) is untouchable.
   c) LABELS MUST AVOID PORTRAIT DISCS. Text on top of a person bubble is worse than text on text, because the disc becomes a real photo later. Give labels a hard avoid-rule against person nodes.
   All three must stay deterministic under the same seed, and must NOT re-shuffle an existing net.

3. SHOW THE DENSITY DIAL — this may already solve much of the crowding, and Birk has never seen it. The seeded graph has 75 terms, 25 of which are mentioned exactly once. Render the SAME graph at min_mentions = 1, 2 and 3, everything else identical, so the effect of the dial alone is visible. Use the real display-filter code path (Task 13), not a special-case renderer. Report how many term nodes and edges remain at each setting.

Filenames must be self-explanatory. Print absolute paths plus a one-line description each, and report the label-overlap improvement with a measured number (e.g. count of overlapping label pairs before/after the declutter pass).

Then stop and report. Implementers on sonnet/haiku; opus only for hard review.
