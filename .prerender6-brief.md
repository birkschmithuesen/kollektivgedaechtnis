Sixth pre-render round — SHORT. Just re-render existing series with the corrected pure-black/pure-white palette. Do not touch plan Tasks 18/19, do not change layout or colour logic further.

Birk approved the colour correction (#000000 background, #FFFFFF labels, gold ring kept) and now wants the earlier still series re-rendered with it, because every still he has seen so far used the old tinted palette.

Deliver into out/prerender6/, all 1920x1080, deterministic, same seeded graph as before:

## 1. The legibility ladder (was prerender2's series A/B/C)
All three type sizes, all pure black/white now:
- 22px labels (variant A)
- 32px labels (variant B — Birk's pick)
- 44px labels (variant C)
Same placement across all three, so only type size and stroke weight differ.

## 2. The test pattern (variant D)
Greyscale wedge + font-size ladder. On a pure-black ground now, so the wedge's black end IS the true projector black — that is exactly what it must measure on site.

## 3. The density dial (was prerender4)
min_mentions = 1 / 2 / 3, with the fcose re-layout so the freed space is used and the type grows.

## 4. The fill-the-screen proof (was prerender4)
~5 persons, ~20 persons, 50 persons — each filling the canvas with type scaled accordingly.

Filenames self-explanatory, absolute paths + one line each in the report.

Also state, per series, whether anything about the picture changed BEYOND the palette (it should not — if a series looks different in layout or scale from its prerender2/prerender4 counterpart, say so and explain why, because that would mean something else moved).

Then stop. Implementers on sonnet/haiku; this is mechanical re-rendering, no design work.
